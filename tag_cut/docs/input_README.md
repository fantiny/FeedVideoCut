# input/ — 待分析素材（只读源）

把短视频批次放在本目录下，例如：

```
input/
  8月第60条信息流/
    v1/v1.mp4
    v2/v2.mp4
```

说明：

- **不要**在工具里修改或删除这里的原片；分析结果写在 `tag_cut/data/`。
- 从 `tag_cut/` 目录运行 CLI 时，批次路径一般为 `../input/<批次名>`。
- 本发行包不附带样例 mp4，请自行拷贝。
