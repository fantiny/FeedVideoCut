# Agent规范（精简版）

## 必须(MUST)与禁止(MUST NOT)
- 结论需证据(日志/文档/测试)，不确定即问，错误透明并记录。
- 禁止编造/隐瞒/臆测/默认开发环境即生产。

## 环境
脚手架，锁依赖选LTS，查兼容性，运行env-check，重复任务脚本化(tools/)，维护docs/TOOLS.md。

## 需求
先调研(≥2权威源)再计划，明确部署环境(OS/网络/资源/并发)以指导选型，不明确即问；变更需全局影响评估。

## 设计
KISS/DRY/YAGNI，SOLID，契约优先，模块分治接口冻结，零技术债。多方案用Provider记利弊。技术栈目录冻结，变更需评估。选驱动模式(数据/UI/事件)，定义数据流，CLI优先。

## 实现
API查文档，禁硬编码敏感，函数定义边界并写异常测试。

## 重构
小步重构，测试通过，更新文档；文档精简(仅ARCH/STACK/TOOLS/ADR)，定期清理。

## 调试
复现→日志按traceId→还原链路→定位，关键路径结构化日志(traceId,耗时)。

## 安全
依赖安全审计，高危修复；优化需基准对比。

## 错误记忆
分级：L1工具(lint/hook)，L2项目(CHECKLIST.md)，L3架构(ADR)，24小时内固化。

## 基础设施
docs/:STACK.md,TOOLS.md,CHECKLIST.md,ARCH.md,ADR/；tools/脚本（含一键启动/停止用于用户验证）；config/集中存放所有配置文件，通过统一工具管理；.env.example。初始化自动生成。

## 量化指标
单元测试≥70%(核心≥90%)，圈复杂度≤10，单文件≤500行；CI全通过；文档≤代码文件10%。